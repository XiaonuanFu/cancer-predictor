# Daily Work Log - 2026-05-23

**Project:** Cancer Predictor  
**Date:** May 23, 2026  
**Focus:** Project documentation structure, Web code workflow, environment inventory, data source inventory, and prompt logging

---

## Summary

今天主要把项目的 `doc/` 文档体系初始化起来，按癌症信息分析项目的实际代码和数据情况，建立了需求、Web 设计、运行环境、数据来源、提示词记录等目录。过程中参考了当前仓库里的 Docker 配置、Web 代码、TCGA 导入脚本、已有日报和本地数据库状态，并把关键工作流程写入文档。

---

## Work Completed

- 初始化 `doc/` 文档总入口。
  - 新增 `doc/README.md`。
  - 说明了文档目录的用途和项目当前结构。
  - 记录了本地源码、原始数据、运行存储和 Web 部署副本之间的关系。

- 新增需求文档目录。
  - 新增 `doc/requirements/README.md`。
  - 新增 `doc/requirements/product-requirements.md`。
  - 初步整理项目目标、当前范围、主要用户、功能需求、非功能需求和开放问题。

- 新增 Web 设计文档目录。
  - 新增 `doc/web-design/README.md`。
  - 新增 `doc/web-design/web-code-design.md`。
  - 记录当前 `web_code/` 的技术栈：Express、`pg`、原生 HTML/CSS/JS。
  - 记录当前接口：`/api/health`、`/api/tables`、`/api/tables/:schema/:name/sample`、`/api/analysis/overview`。
  - 记录 Web 页面三块主导航：`Cancer Data`、`Chemical Data`、`Data Analysis`。

- 补充 Web 代码发布工作流程。
  - 明确本地 Web 源码目录是 `web_code/`。
  - 明确写完代码后，还需要同步到 `docker_storage/jupyter/node-app`。
  - 该目录在 Jupyter 容器内对应 `/workspace/node-app`，由容器内 Node 服务运行。
  - 后续 Web 代码修改应同时保证源码目录和容器 Node 服务目录一致。

- 新增环境和数据库文档目录。
  - 新增 `doc/environment/README.md`。
  - 新增 `doc/environment/current-container-and-database.md`。
  - 记录 Docker Compose 服务、容器、镜像、端口、Jupyter token、Node 服务目录和 PostgreSQL 连接信息。

- 实际检查当前本地容器和数据库状态。
  - `bio-jupyter` 正在运行，使用 `bio:latest`。
  - `bio-postgres` 正在运行，使用 `bio-postgres:18`。
  - PostgreSQL 版本为 `18.4`。
  - `bio` 数据库约 55 GB。
  - 当前主要 schema：
    - `bio_tcga`：28 张表，约 29 GB。
    - `chembl`：74 张表，约 38 GB。
  - 已安装扩展包括 `postgis`、`vector`、`rdkit`、`pg_trgm`、`fuzzystrmatch`。

- 新增数据来源文档目录。
  - 新增 `doc/data-sources/README.md`。
  - 新增 `doc/data-sources/data-inventory.md`。
  - 记录 `data/raw_data/` 中 TCGA/PanCanAtlas 相关文件和 `bio_tcga` 表之间的映射。
  - 记录 `data/chembl/` 中 ChEMBL 36 dump 文件和 `chembl` schema 情况。
  - 记录 TCGA 导入脚本 `scripts/import_tcga_raw_data.py` 的导入策略和 `bio_tcga.import_manifest` 来源追踪表。

- 新增提示词记录目录。
  - 新增 `doc/prompts/README.md`。
  - 新增 `doc/prompts/2026-05-23.md`。
  - 约定每天按 `doc/prompts/YYYY-MM-DD.md` 整理当天用过的 prompt。
  - 模板包含原始 prompt、分类、上下文、结果、复用决策和备注。

---

## Files Added / Updated

- `doc/README.md`
- `doc/requirements/README.md`
- `doc/requirements/product-requirements.md`
- `doc/web-design/README.md`
- `doc/web-design/web-code-design.md`
- `doc/environment/README.md`
- `doc/environment/current-container-and-database.md`
- `doc/data-sources/README.md`
- `doc/data-sources/data-inventory.md`
- `doc/prompts/README.md`
- `doc/prompts/2026-05-23.md`
- `reports/daily/2026-05-23.md`

---

## Issues / Notes

- `doc/` 目录之前为空，今天完成了第一版结构初始化。
- 当前文档中的数据来源主要来自本地文件名、已有日报、导入脚本和数据库状态检查。
- 数据来源文档里已经标注：正式发布或共享分析结果前，还需要补充官方下载链接、访问日期、校验和、许可证说明。
- Web 代码工作流已明确：不能只更新 `web_code/`，还要同步到 Jupyter 容器 Node 服务目录。

---

## Useful Commands

```bash
# 查看文档文件
find doc -maxdepth 3 -type f | sort

# 启动本地服务
docker compose up -d

# 查看容器状态
docker ps

# 进入 PostgreSQL
docker exec -it bio-postgres psql -U bio -d bio

# 访问 Jupyter
open http://127.0.0.1:8888/lab?token=bioanalysis

# 访问 Node Web app
open http://127.0.0.1:3000/
```

---

## Next Steps

- 后续每次修改 Web 应用后，同步 `web_code/` 到 `docker_storage/jupyter/node-app` 并验证 `http://127.0.0.1:3000/`。
- 每天工作结束后，把当天用过的 prompt 整理到 `doc/prompts/YYYY-MM-DD.md`。
- 继续完善数据来源文档，补充官方链接、下载日期、checksum 和许可信息。
- 根据第一个癌症预测目标，进一步细化需求文档和分析任务列表。

