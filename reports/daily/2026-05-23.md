# Daily Work Log - 2026-05-23

**Project:** Cancer Predictor  
**Date:** May 23, 2026  
**Focus:** Project documentation structure, web code workflow, environment inventory, data source inventory, and prompt logging

---

## Summary

Today I initialized the project documentation structure under `doc/` based on the current cancer information analysis code and data layout. The new structure includes requirements, web design, runtime environment, data sources, and prompt logs. I referenced the repository's Docker configuration, web code, TCGA import scripts, existing daily reports, and local database state, then documented the key workflows.

---

## Work Completed

- Initialized the main `doc/` entry point.
  - Added `doc/README.md`.
  - Explained the purpose of the documentation directory and the current project structure.
  - Recorded the relationship among local source code, raw data, runtime storage, and the web deployment copy.

- Added the requirements documentation directory.
  - Added `doc/requirements/README.md`.
  - Added `doc/requirements/product-requirements.md`.
  - Organized the initial project goals, current scope, main users, functional requirements, non-functional requirements, and open questions.

- Added the web design documentation directory.
  - Added `doc/web-design/README.md`.
  - Added `doc/web-design/web-code-design.md`.
  - Documented the current `web_code/` stack: Express, `pg`, and native HTML/CSS/JS.
  - Documented the current APIs: `/api/health`, `/api/tables`, `/api/tables/:schema/:name/sample`, and `/api/analysis/overview`.
  - Documented the three main web page navigation sections: `Cancer Data`, `Chemical Data`, and `Data Analysis`.

- Added the web code release workflow.
  - Confirmed that the local web source directory is `web_code/`.
  - Confirmed that code changes also need to be synced to `docker_storage/jupyter/node-app`.
  - This directory maps to `/workspace/node-app` inside the Jupyter container and is served by the container's Node service.
  - Future web code changes should keep both the source directory and the container Node service directory in sync.

- Added environment and database documentation.
  - Added `doc/environment/README.md`.
  - Added `doc/environment/current-container-and-database.md`.
  - Recorded Docker Compose services, containers, images, ports, the Jupyter token, the Node service directory, and PostgreSQL connection information.

- Checked the current local container and database state.
  - `bio-jupyter` is running and uses `bio:latest`.
  - `bio-postgres` is running and uses `bio-postgres:18`.
  - PostgreSQL version is `18.4`.
  - The `bio` database is about 55 GB.
  - Main schemas:
    - `bio_tcga`: 28 tables, about 29 GB.
    - `chembl`: 74 tables, about 38 GB.
  - Installed extensions include `postgis`, `vector`, `rdkit`, `pg_trgm`, and `fuzzystrmatch`.

- Added data source documentation.
  - Added `doc/data-sources/README.md`.
  - Added `doc/data-sources/data-inventory.md`.
  - Recorded the mapping between TCGA/PanCanAtlas files in `data/raw_data/` and tables in `bio_tcga`.
  - Recorded the ChEMBL 36 dump files in `data/chembl/` and the `chembl` schema status.
  - Recorded the import strategy in `scripts/import_tcga_raw_data.py` and the provenance table `bio_tcga.import_manifest`.

- Added prompt logging documentation.
  - Added `doc/prompts/README.md`.
  - Added `doc/prompts/2026-05-23.md`.
  - Set the convention for saving each day's prompts as `doc/prompts/YYYY-MM-DD.md`.
  - The template includes original prompt, category, context, result, reuse decision, and notes.

---

## Files Added / Updated

- `doc/README.md`
- `doc/requirements/README.md`
- `doc/requirements/product-requirements.md`
- `doc/web-design/README.md`
- `doc/web-design/web-code-design.md`
- `doc/environment/README.md`
- `doc/environment/current-container-and-database.md`
- `doc/data-sources/README.md`
- `doc/data-sources/data-inventory.md`
- `doc/prompts/README.md`
- `doc/prompts/2026-05-23.md`
- `reports/daily/2026-05-23.md`

---

## Issues / Notes

- The `doc/` directory was previously empty; the first documentation structure was completed today.
- The current data source documentation is based mainly on local file names, existing daily reports, import scripts, and database-state checks.
- The data source document already notes that official download links, access dates, checksums, and license information should be added before formal release or sharing of analysis results.
- The web code workflow is now explicit: updating only `web_code/` is not enough; changes must also be synced to the Jupyter container Node service directory.

---

## Useful Commands

```bash
# View documentation files
find doc -maxdepth 3 -type f | sort

# Start local services
docker compose up -d

# Check container status
docker ps

# Enter PostgreSQL
docker exec -it bio-postgres psql -U bio -d bio

# Open Jupyter
open http://127.0.0.1:8888/lab?token=bioanalysis

# Open the Node web app
open http://127.0.0.1:3000/
```

---

## Next Steps

- After each web app change, sync `web_code/` to `docker_storage/jupyter/node-app` and verify `http://127.0.0.1:3000/`.
- At the end of each workday, organize the prompts used that day into `doc/prompts/YYYY-MM-DD.md`.
- Continue improving the data source documentation with official links, download dates, checksums, and license information.
- Refine requirements and analysis tasks around the first cancer prediction goal.
