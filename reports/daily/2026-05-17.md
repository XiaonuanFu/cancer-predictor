# Daily Work Log - 2026-05-17

**Project:** Cancer Predictor  
**Date:** May 17, 2026  
**Focus:** Local development environment setup, PostgreSQL database setup, and DBeaver connection setup

---

## Summary

Today I synchronized the local Git repository with GitHub, rebuilt the local bioinformatics Docker environment, created a PostgreSQL 18 database container with required scientific extensions, and configured DBeaver Community to connect to the local database.

---

## Work Completed

- Synced the local `main` branch with GitHub.
  - Pulled 2 remote commits from `origin/main`.
  - Rebasing completed successfully.
  - Pushed the local commit back to GitHub.
  - Left local `.DS_Store` files uncommitted.

- Built the main bioinformatics Docker image.
  - Final image name: `bio:latest`.
  - Started JupyterLab container: `bio-jupyter`.
  - JupyterLab URL: `http://127.0.0.1:8888/lab?token=bioanalysis`.
  - Verified Python packages including `pandas`, `numpy`, `sklearn`, `Bio`, and `rdkit`.

- Built the PostgreSQL 18 image.
  - Final image name: `bio-postgres:18`.
  - Started PostgreSQL container: `bio-postgres`.
  - Database volume: `bio-postgres-data:/var/lib/postgresql`.
  - Connection:
    - Host: `127.0.0.1`
    - Port: `5432`
    - Database: `bio`
    - User: `bio`
    - Password: `bioanalysis`

- Installed and verified PostgreSQL extensions.
  - `postgis` version `3.6.3`
  - `vector` version `0.8.2`
  - `rdkit` version `4.7.0`
  - `pg_trgm` version `1.6`
  - `fuzzystrmatch` version `1.2`

- Configured DBeaver Community.
  - Created a local connection named `bio local PostgreSQL 18`.
  - Configured it to connect to the `bio` database on `127.0.0.1:5432`.
  - Verified the database is reachable from the Docker container.

---

## Issues / Notes

- Docker Hub and default Debian apt sources were slow or unstable at first.
  - Used temporary Dockerfiles with faster mirror sources for building images.
  - The project Dockerfiles were not overwritten during this process.

- PostgreSQL 18 Docker image uses a new data directory convention.
  - Mounting the volume at `/var/lib/postgresql/data` caused an initialization error.
  - Fixed by mounting the volume at `/var/lib/postgresql`.

- GitHub warned that `bio-exercise/exercise1/chr20.fa` is larger than 50 MB.
  - It may be worth using Git LFS for large genomic files later.

---

## Useful Commands

```bash
# Jupyter environment
docker start bio-jupyter
docker stop bio-jupyter

# PostgreSQL database
docker start bio-postgres
docker stop bio-postgres
docker exec -it bio-postgres psql -U bio -d bio

# Check running containers
docker ps
```

---

## Next Steps

- Use DBeaver to inspect the `bio` database and practice running SQL queries.
- Decide whether large genomic files should move to Git LFS.
- Continue documenting daily work in `reports/daily/`.

---

## Daily Log Template

```markdown
# Daily Work Log - YYYY-MM-DD

## Summary

## Work Completed

## Issues / Notes

## Useful Commands / Links

## Next Steps
```
